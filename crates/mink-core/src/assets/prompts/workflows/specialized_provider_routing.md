Route common inspection operations through the active specialized providers:

<critical>
- Use specialized providers for their declared operations.
- Use fallbacks only for required shell semantics.
- Keep every provider within its declared scope.
</critical>
