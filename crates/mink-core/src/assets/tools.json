[
  {
    "name": "Read",
    "description": "Read one local or configured virtual-filesystem path or registered lightweight resource. Use multiple calls for multiple targets. Local non-raw reads include numbered lines and an @PATH#TAG editable snapshot header; raw, virtual-filesystem, and registered-resource reads never create an editable snapshot. Path selectors select line ranges.\n\nSupported targets include local paths, artifact://<id>, skill:// resources, rule:// resources, and session://current resources. A full result may be truncated; request a narrower selector when exact lines are known.",
    "input_schema": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "File path or registered resource URL to read. Supports local paths, artifact://<id>, skill://list, skill://list/all, skill://<name>, skill://<name>/<relative-path>, rule://<name>, and session://current resources including session://current/history. Append path selectors such as :10-20, :10+5, or :raw for ranges/raw output."
        }
      },
      "required": [
        "path"
      ]
    }
  },
  {
    "name": "Glob",
    "description": "Discover paths through the configured filesystem backend with ripgrep-style rg --files -g semantics. Patterns are passed as a single rg -g override glob: *.rs matches recursively by file name, src/*.rs matches one level under src, src/**/*.rs matches recursively, and !pattern excludes matches. Use multiple calls for independent patterns.",
    "input_schema": {
      "type": "object",
      "properties": {
        "pattern": {
          "type": "string",
          "description": "A single rg -g override glob, for example *.rs, **/*.rs, **/*.docx, **/*.*, src/**/*.ts, or !target/**. Bare file-name globs such as *.rs match recursively like ripgrep."
        },
        "path": {
          "type": "string",
          "description": "Optional directory to search from. Defaults to ."
        }
      },
      "required": [
        "pattern"
      ]
    }
  },
  {
    "name": "Grep",
    "description": "Search content through the configured filesystem backend or a registered resource URL such as session://current/history. Uses ripgrep-compatible regex/search/printer behavior and returns path:line:match output, context lines, and -- between context groups. Resource paths do not accept selectors or globs. Use context to identify exact target ranges, a single rg -g override glob to restrict filesystem targets, and multiple calls for independent patterns.",
    "input_schema": {
      "type": "object",
      "properties": {
        "pattern": {
          "type": "string",
          "description": "The Rust regular expression pattern to search for. For literal symbols, simple names usually work directly; escape regex metacharacters when needed."
        },
        "path": {
          "type": "string",
          "description": "File, directory, or registered resource URL to search. Defaults to ."
        },
        "glob": {
          "type": "string",
          "description": "A single rg -g override glob to filter files (e.g. \"*.rs\", \"**/*.docx\", \"src/**/*.ts\", \"*.{ts,tsx}\", or \"!target/**\"). Bare file-name globs such as *.rs match recursively like ripgrep."
        },
        "context": {
          "type": "integer",
          "description": "Number of lines before and after each match. Search output never provides an editable @PATH#TAG."
        }
      },
      "required": [
        "pattern"
      ]
    }
  },
  {
    "name": "Write",
    "description": "Create or fully overwrite a file on the local filesystem. Parent directories are created automatically. This operation never writes to an injected read-only virtual filesystem.",
    "input_schema": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Path to the file to write"
        },
        "content": {
          "type": "string",
          "description": "Content to write to the file"
        }
      },
      "required": [
        "path",
        "content"
      ]
    }
  },
  {
    "name": "Edit",
    "description": "Apply an anchored patch to an existing local file. The patch must begin with a fresh @PATH#TAG editable snapshot header for the same path; stale, missing, uncovered, overlapping, or no-op patches fail closed. A successful operation returns a fresh header and numbered lines around the change. Combine multiple ranges grounded in one snapshot into one patch, keep ranges tight, and prefix every final body row with +. old_string/new_string and context-diff rows are not supported.",
    "input_schema": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Path to the file to edit"
        },
        "patch": {
          "type": "string",
          "description": "Anchored line patch. The first non-blank line must be a fresh @PATH#TAG editable snapshot header for the same path. Supports replace N..M:, replace N:, delete N..M, delete N, insert before N:, insert after N:, insert head:, and insert tail:. Numbers refer to original snapshot lines and do not shift within one patch. Body rows are final new lines prefixed with +; never include original lines, -old rows, or bare context lines. Example: @src/foo.rs#0A3B\\nreplace 5..5:\\n+    return result"
        }
      },
      "required": [
        "path",
        "patch"
      ]
    }
  },
  {
    "name": "Bash",
    "description": "Execute a focused shell command in the current session working directory for build, test, version-control, package-manager, server, or other shell-only tasks. Returns stdout and stderr. Commands classified as ordinary file reading, content search, or path discovery may be rejected by runtime policy.",
    "input_schema": {
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "description": "The focused shell command to execute."
        },
        "timeout": {
          "type": "integer",
          "description": "Timeout in seconds. If the command exceeds this duration, it will be killed. Only set this for commands expected to need a non-default duration (e.g. build, test, server start). Default: global tool timeout from configuration, clamped to 5-600 seconds."
        }
      },
      "required": [
        "command"
      ]
    }
  },
  {
    "name": "Python",
    "description": "Execute Python code directly on the host system with network, subprocess, filesystem, and C-extension access. Returns stdout and stderr. Suitable for data processing, analysis, or helper scripts that require the host Python ecosystem.",
    "input_schema": {
      "type": "object",
      "properties": {
        "script": {
          "type": "string",
          "description": "Inline Python code to execute."
        },
        "script_file": {
          "type": "string",
          "description": "Path to a Python file to execute. Provide either 'script' or 'script_file', not both."
        },
        "timeout": {
          "type": "integer",
          "description": "Timeout in seconds (default: 30, min: 5, max: 300)."
        }
      }
    }
  },
  {
    "name": "TodoRead",
    "description": "Read the persisted task list for the current session, including its revision and stable item IDs. Completed items are omitted by default.",
    "input_schema": {
      "type": "object",
      "properties": {
        "include_completed": {
          "type": "boolean",
          "description": "Whether to include completed items in the result. Defaults to false."
        }
      },
      "additionalProperties": false
    }
  },
  {
    "name": "TodoWrite",
    "description": "Apply an atomic incremental update to the persisted task list for the current session. Updates use the current revision and stable item IDs to prevent stale writes.",
    "input_schema": {
      "type": "object",
      "properties": {
        "base_revision": {
          "type": "integer",
          "minimum": 0,
          "description": "The revision on which this update is based."
        },
        "add": {
          "type": "array",
          "description": "New items to append. Every new item starts pending.",
          "items": {
            "type": "object",
            "properties": {
              "content": {
                "type": "string",
                "description": "A short, concrete, actionable task description."
              }
            },
            "required": ["content"],
            "additionalProperties": false
          }
        },
        "update": {
          "type": "array",
          "description": "Content replacements for existing non-completed items identified by stable ID.",
          "items": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string",
                "description": "Stable task ID."
              },
              "content": {
                "type": "string",
                "description": "Replacement task description."
              }
            },
            "required": ["id", "content"],
            "additionalProperties": false
          }
        },
        "remove": {
          "type": "array",
          "description": "Stable IDs of non-active items to delete. Pause or complete active items first.",
          "items": {
            "type": "string"
          }
        }
      },
      "required": ["base_revision"],
      "additionalProperties": false
    }
  },
  {
    "name": "TodoAdvance",
    "description": "Atomically transition persisted todo progress. Complete or pause active items, activate pending items, or reopen completed items. Multiple related items may transition together.",
    "input_schema": {
      "type": "object",
      "properties": {
        "base_revision": {
          "type": "integer",
          "minimum": 0,
          "description": "The revision on which these transitions are based."
        },
        "complete": {
          "type": "array",
          "description": "In-progress item IDs whose outcomes have been verified.",
          "items": {"type": "string"}
        },
        "activate": {
          "type": "array",
          "description": "Pending item IDs to make part of the active batch.",
          "items": {"type": "string"}
        },
        "pause": {
          "type": "array",
          "description": "In-progress item IDs to return to pending.",
          "items": {"type": "string"}
        },
        "reopen": {
          "type": "array",
          "description": "Completed item IDs to return to pending.",
          "items": {"type": "string"}
        }
      },
      "required": ["base_revision"],
      "additionalProperties": false
    }
  },
  {
    "name": "PlanDraft",
    "description": "Create, replace, or cancel the current plan draft. Use this for the complete proposed plan and for every revision before explicit user confirmation. Pass an empty content string when the user explicitly cancels or abandons planning. This tool does not confirm the plan.",
    "input_schema": {
      "type": "object",
      "properties": {
        "content": {
          "type": "string",
          "description": "The complete plan draft in Markdown. Pass an empty string to cancel the unconfirmed draft."
        }
      },
      "required": [
        "content"
      ]
    }
  },
  {
    "name": "PlanConfirm",
    "description": "Confirm and lock in the current plan draft. Call this ONLY when the user explicitly confirms the plan. Do NOT call this during drafting or when the user requests changes \u2014 only on explicit confirmation.",
    "input_schema": {
      "type": "object",
      "properties": {}
    }
  },
  {
    "name": "PlanClear",
    "description": "Clear the current plan. Call this when the current plan is fully completed and all tasks are done. After calling this, the system prompt will no longer include the plan section.",
    "input_schema": {
      "type": "object",
      "properties": {}
    }
  },
  {
    "name": "SubAgent",
    "description": "Launches a sub-agent that runs independently. The sub-agent executes the given prompt as an isolated agent session. Results are batched: ALL sub-agents complete before results are presented together. Use this for delegating sub-tasks that can run in parallel.\n\nFork mode: pass fork=true to inherit parent session context (conversation history, plan, skills). Default is a fresh independent session.",
    "input_schema": {
      "type": "object",
      "properties": {
        "prompt": {
          "type": "string",
          "description": "The prompt/task to delegate to the sub-agent"
        },
        "description": {
          "type": "string",
          "description": "Short description of the sub-agent task (for logging)"
        },
        "fork": {
          "type": "boolean",
          "description": "Set to true to inherit parent session context (conversation history, plan, skills). Default is false for a fresh independent session."
        }
      },
      "required": ["prompt"]
    }
  },
  {
    "name": "PythonSandbox",
    "description": "Execute Python code in a CPython WASI sandbox with restricted permissions — only specific directories have read/write access. No subprocess, no network, no C extensions. Full CPython standard library available (json/csv/re/math/datetime/xml etc.). Use this for safe execution of untrusted or sensitive Python code.",
    "input_schema": {
      "type": "object",
      "properties": {
        "script": {
          "type": "string",
          "description": "Inline Python code to execute."
        },
        "script_file": {
          "type": "string",
          "description": "Path to a Python file to execute. Provide either 'script' or 'script_file', not both."
        },
        "timeout": {
          "type": "integer",
          "description": "Timeout in seconds (default: 30, min: 5, max: 300)."
        }
      }
    }
    }
]
